<div class="row">
  <div class="container col-md-6 col-md-offset-3">

<h1 class="heady"><%= @creature.name %></h1>


<p class="textty">Description:<br> <%= @creature.desc %> <br> Tags: <br>

 <!-- <%= @creature.tags.map{|t| t.name}.join(', ') %> -->

  <% @tag.each do |t| %>
      <li class="creatures"><%= link_to t.name, t %></li>
    <% end %>



</p>

<!--
  <%=  @creature.tags.each do |t|

  link_to t.name, tag_path(t)

 end %>
 -->

<img src=<%= @photo %>>

<br>
<%= link_to "Edit Creature", edit_creature_path, :class=>"btn btn-success" %>
<%= link_to "Remove Creature", @creature, :method => :delete, :class=>"btn btn-danger" %>
</div>
</div>


